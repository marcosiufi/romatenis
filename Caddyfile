# Caddy gera certificados Let's Encrypt automaticamente.
# Pré-requisito: DNS do domínio apontando para este servidor.

# Redireciona www → raiz
www.{$DOMAIN} {
    redir https://{$DOMAIN}{uri} permanent
}

# App principal
{$DOMAIN} {
    reverse_proxy api:8000
    encode gzip zstd
}
